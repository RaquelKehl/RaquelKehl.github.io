version: 2
updates:
  # keep the deploy workflow's actions current
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels: ["dependencies"]

  # NOTE: re-add a "pip" entry here when the CrewAI Python system
  # (requirements.txt) lands — with no manifest present the check only fails.
